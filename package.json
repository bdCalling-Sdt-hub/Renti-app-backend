{
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-useragent": "^1.0.15",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.4.3",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.4",
    "nodemon": "^3.0.1",
    "stripe": "^13.2.0",
    "uuid": "^9.0.0"
  },
  "name": "renti-backend",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "build": "npm run build-server",
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-server": "mkdir build && cp -r controllers/* build && cp -r middleWares/* build && cp -r models/* build && cp -r public/* build && cp -r routes/* build && cp -r services/* build && cp -r app build && rm -rf build/static/js/*.map && rm -rf build/static/css/*.map"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
